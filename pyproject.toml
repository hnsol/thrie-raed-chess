[project]
name = "movesense-chess"
version = "0.1.0"
description = "3択で覚えるチェス"
requires-python = ">=3.11"
dependencies = [
    "chess>=1.10",
    "textual>=0.60",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
